% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viewr_options.R
\name{viewdt_options}
\alias{viewdt_options}
\title{Configure the ViewR data explorer}
\usage{
viewdt_options(
  theme = c("auto", "light", "dark"),
  show_labels = TRUE,
  histograms = TRUE,
  missing_bars = TRUE,
  type_badges = TRUE,
  insights = TRUE,
  query_builder = TRUE,
  column_picker = TRUE,
  code_export = TRUE,
  global_search = TRUE,
  na_string = "NA",
  hidden_columns = NULL,
  page_size = 200L,
  hist_bins = 20L,
  top_n = 10L,
  max_cells = 5e+06
)
}
\arguments{
\item{theme}{UI appearance: one of `"auto"` (default, follows the
host/system colour scheme), `"light"`, or `"dark"`.}

\item{show_labels}{Logical. Display variable-label attributes (e.g. as
set by \pkg{haven} or clinical ADaM data) inline in the column headers.
Default `TRUE`.}

\item{histograms}{Logical. Render mini spark-histograms / category
bars inside the column headers. Default `TRUE`.}

\item{missing_bars}{Logical. Render the data-completeness (missingness)
bar at the bottom of each header. Default `TRUE`.}

\item{type_badges}{Logical. Show data-type badges in headers.
Default `TRUE`.}

\item{insights}{Logical. Enable the sliding Data Insights drawer.
Default `TRUE`.}

\item{query_builder}{Logical. Enable the multi-condition visual query
builder. Default `TRUE`.}

\item{column_picker}{Logical. Enable the column-visibility picker.
Default `TRUE`.}

\item{code_export}{Logical. Enable the reproducible code generator
(dplyr / base R / SQL). Default `TRUE`.}

\item{global_search}{Logical. Enable the global search box. Default `TRUE`.}

\item{na_string}{Character. Placeholder shown for missing values.
Default `"NA"`.}

\item{hidden_columns}{Character vector of column names hidden on first
render. Default `NULL`.}

\item{page_size}{Integer. Rows kept in the virtualized DOM buffer.
Default `200L`.}

\item{hist_bins}{Integer. Number of bins for numeric histograms.
Default `20L`.}

\item{top_n}{Integer. Number of categories profiled for character
columns. Default `10L`.}

\item{max_cells}{Integer. Soft safeguard; data frames with more than
this many cells (`nrow * ncol`) trigger a warning. Default `5e6`.}
}
\value{
A named list of class `"viewdt_options"`.
}
\description{
Builds the option list consumed by [viewdt()] and [save_viewdt()]. Every
argument has a sensible default, so `viewdt_options()` with no arguments
returns a fully usable configuration.
}
\examples{
viewdt_options(theme = "dark", hidden_columns = c("cyl", "hp"))
}
